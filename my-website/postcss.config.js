module.exports = {
  plugins: [
    require('autoprefixer'), // Automatically add vendor prefixes
    require('tailwindcss'), // Include Tailwind CSS if you're using it
    // Add other PostCSS plugins here if needed
  ],
}